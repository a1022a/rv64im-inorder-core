# Baseline frequency sweep

All eleven requested points have qualified mapped implementation evidence. Results are in `frequency_tradeoff.csv` and `setup_boundary.csv`. Area rises as the target tightens, while the critical family migrates; a single critical-path optimization therefore does not define global Fmax.
