# Critical-path migration

At 1.35 ns the worst family was `DIV_RESULT_TO_ID_ALU_OPERAND`. At 1.30 ns it migrated to `ID_ALU_OP_TO_ICACHE_PLRU`; 138 of 149 violating paths (92.62%) belonged to that latter family. At final P2R1 2.0 ns, the worst family is the CSR cycle counter. Critical topology depends on the constraint and mapped implementation.
