# Baseline modeling handoff

This file is retained to mark the baseline handoff point before A2 modeling.
The statement that modeling was future work was true only at that baseline.
The completed attribution, branch, cache, and load-use models now live beside
this file and are summarized by `models/README.md` and `docs/experiments/`.
