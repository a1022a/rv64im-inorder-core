# Legacy synthesis entry point

The active public synthesis and timing methodology is `../eda/`. This directory
is retained only so links from the publication baseline do not break. Do not
run an older flow from here or interpret this directory as a second ASIC flow.

Use `../eda/README.md` for constraints, DC/PT scripts, environment boundaries,
sanitization status, and the supported pre-layout closure claim.
