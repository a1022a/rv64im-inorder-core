# Synthesis Handoff

This directory is a placeholder for future synthesis handoff collateral.

Current status:

- Synthesis top candidate is `rv64im_core_top`.
- Formal synthesis, STA, LEC, and PPA evaluation have not been run.
- RAM synthesis strategy for `S011HD1P_X32Y2D128_BW.sv` is `TBD`.
- `rtl/filelists/synth.f` records the current synthesis-intent RTL scope, but
  does not approve a memory macro, SRAM compiler, black-box, or behavioral RAM
  policy.
- `handoff_manifest.md` records the current placeholder handoff boundary. It is
  not evidence that synthesis, STA, LEC, or PPA evaluation has been run.
