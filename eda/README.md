# Sanitized EDA methodology

The DC/PT scripts are sanitized copies of scripts used for the qualified runs. Machine-local roots, vendor library identities, macro identity, tool installation paths, and license configuration are replaced by environment variables. The constraint file documents the actual block/interface characterization assumptions. It is not a full-chip signoff SDC.
